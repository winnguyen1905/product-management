mixin search(keyword)
    form(action="" id="form-search")
        .input-group 
            input(
                type="text"
                placeholder="Enter Your Keywords"
                name="keyword"
                class="form-control"
                value=keyword
            )
            .input-group-append
                button(
                    class="btn btn-success"
                    type="submit"
                ) Search